# ESTE DIRETORIO ESTA OBSOLETO!

Todos os comandos do projeto, incluindo `npm install` e `npm start`, agora devem ser executados a partir do **diretorio raiz do projeto** (a pasta que contem esta pasta `backend`).

Esta pasta pode ser deletada com seguranca.
